{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Solo el diálogo de guardar del sistema. La escritura la hace un comando propio sobre la ruta que el usuario acaba de señalar, de modo que no hace falta abrir permisos generales al sistema de ficheros.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:default"]
}
